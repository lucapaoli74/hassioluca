# Copy to secrets.yaml (which is gitignored) and fill in your real values.
wifi_ssid: "YourWifiName"
wifi_password: "YourWifiPassword"
# Generate with: esphome secrets or `python3 -c "import base64,os;print(base64.b64encode(os.urandom(32)).decode())"`
api_encryption_key: "REPLACE_WITH_32_BYTE_BASE64_KEY"
ota_password: "ChooseAStrongPassword"
